# Demo (Optional)

Record a 2–3 minute walkthrough:
1) Show the app repo with `.github/workflows/deploy.yml`.
2) Push a small commit.
3) Show the GitHub Actions run starting.
4) Show n8n Executions receiving the payload and the Parse node output.
5) Show Slack/Discord messages.
6) Show Supabase rows with a quick SQL query.

Embed a Loom/YouTube link here when ready.
